alexshelkov | 2023-06-01 11:58:15 UTC | #1

I want to create an [ether](https://ethers.org/) wallet via `new Wallet(key: string)`. Where key is a users `privateKey`. Can I somehow get the this key from the Internet identity? So I can create an ETH wallet based on it.

-------------------------

